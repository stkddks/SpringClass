<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.EventBoardMapper">
<!-- <resultMap type="event" id="boardResult">
		<id property="boardNo" column="BOARD_NO"></id>
		<result property="boardCategory" column="BOARD_CATEGORY" />
		<result property="boardTitle" column="BOARD_TITLE" />
		<result property="boardContent" column="BOARD_CONTENT" />
		<result property="boardRegdate" column="BOARD_REGDATE" />
		<result property="boardReadcount" column="READCOUNT" />
		<result property="fileName" column="FILE_NAME" />
		<result property="thumbnail" column="THUMBNAIL" />
		<result property="userNo" column="USER_NO" />
	</resultMap> -->
	<insert id="register">
		INSERT INTO KOCO_BOARD(BOARD_CATEGORY, BOARD_TITLE, BOARD_CONTENT, BOARD_REGDATE, READCOUNT, FILE_NAME, THUMBNAIL, USER_NO)
		VALUES(#{boardCategory}, #{boardTitle}, #{boardContent}, NOW(), #{boardReadcount}, #{fileName, jdbcType=VARCHAR}, NULL, #{userNo})
	</insert>
	<select id="get" resultType="org.zerock.domain.EventVO">
		select * from KOCO_BOARD where BOARD_NO=#{boardNo}
	</select>
	<update id="update">
		update KOCO_BOARD 
		set BOARD_TITLE=#{boardTitle},BOARD_CONTENT=#{boardContent},BOARD_REGDATE=NOW(),FILE_NAME=#{fileName},THUMBNAIL=#{thumbnail},USER_NO=#{userNo} where BOARD_NO=#{boardNo}
	</update>
	<delete id="delete">
		delete from KOCO_BOARD where BOARD_NO=#{boardNo}
	</delete>
	<!-- <select id="getList" resultType="org.zerock.domain.EventVO">
		SELECT BOARD_NO, BOARD_TITLE, USER_NO, BOARD_REGDATE, READCOUNT FROM KOCO_BOARD
	</select> -->
	<select id="getListWithPaging" resultType="org.zerock.domain.EventVO">
		select BOARD_NO,BOARD_TITLE,USER_NO,BOARD_REGDATE,READCOUNT
		from KOCO_BOARD
		<!-- order by boardNo desc -->
		limit #{pageNum}, #{amount}
	</select>
	<select id="getTotalCount" resultType="int">
		select count(*) 
		from KOCO_BOARD 
		where BOARD_NO > 0
	</select>
</mapper>